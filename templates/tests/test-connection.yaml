apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pyspark.fullname" . }}-test-connection"
  labels:
    {{- include "pyspark.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pyspark.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
